.dashboard-container {
  display: flex;
  /* height: 100%; */
  transition: all 0.3s ease;
  /* justify-content: flex-start; Align items from left */
  margin-top: 1px; /* Push content below fixed navbar */
  /* width: 100%; */
}
/* Sidebar visible */
.sidebar {
  display: block;
  position: fixed;
  top: 50px;  /* Ensure it starts below the navbar */
  /* left: 0; */
  width: 250px;  /* Sidebar width */
  height: 100%;
  margin-left: -50px;
  background-color: #0c2640;

  transition: transform 0.5s ease-in-out;  /* Sidebar transition */
  transform: translateX(0); /* Default position */
  /* z-index: 15; Ensure it's above other content */
  /* padding-right: 100px; */
}

/* Sidebar hidden */
.sidebar.hidden {
  transform: translateX(-100%);  /* Sidebar hidden off-screen */
}

/* Navbar styling */
.navbar {
  position: fixed;
  top: 0;
  width: 100%;
  z-index: 20; /* Ensure navbar stays above everything else */
  /* padding: 10px 20px; */
  display: flex;
  justify-content: space-evenly;
  align-items: center;
}

/* Layout container for dashboard */


/* Main container styles */
 
.div{
  align-items: center;
  justify-content: center;
  /* flex-grow: 1; */

}

/* Adjust Main Container When Sidebar is Hidden */
.sidebar-hidden .main-container {
  margin-left: -50px;
  /* width: 100%; Full width when sidebar is hidden */
}
/* Media Query for smaller screens */
@media screen and (max-width: 768px) {
  /* Stack sidebar and main container on small screens */
  .dashboard-container {
    flex-direction: column; /* Stack vertically */
  }

  /* Sidebar width adjustment for mobile screens */
  .sidebar {
    width: 145px; /* Sidebar takes full width on mobile */
    position: relative; /* Remove fixed position */
    top: 0;
    /* height: 100vh; Full viewport height */

    transform: translateX(0); /* Ensure it stays visible */
  }

  /* Navbar adjusts to fit the screen */
  .navbar {
    padding: 15px;
  }

  /* .main-container{
    margin-left: 165px;
  } */
}

/* Container styling for the input box and button */
.search-container {
  position: absolute;

  /* position: fixed; */
  bottom: 20px; /* Anchor to 20px from the bottom of the viewport */
  left: 50%; /* Center horizontally relative to the viewport */
  transform: translateX(-50%); /* Shift left by 50% of its own width to center */
  
  display: flex;
  align-items: center; /* Align items vertically within the flex container */
  justify-content: center; /* Center items horizontally within the flex container */
  gap: 10px;
  /* background-color: #0c2640; */
  /* border: 1px solid #ddd; */
  border-radius: 25px;
  padding: 10px 15px;
  box-shadow: 0px 4px 6px rgba(0, 0, 0, 0.1);

  width: 90%; /* Responsive width */
  max-width: 600px; /* Maximum width to avoid excessive size */
  transition: transform 0.3s ease-in-out; /* Smooth transition */
}

/* Adjust search-container when sidebar is hidden */
/* Adjust Search Container When Sidebar is Hidden */
.dashboard-container.sidebar-hidden .search-container {
  width: calc(100% - 230px); /* Adjust width based on sidebar visibility */
  left: 50%; /* Keep horizontally centered */
  transform: translateX(-50%); /* Ensure proper centering */
}

/* Search input box styling */
.search-box {
  flex: 1;
  border: none;
  outline: none;
  font-size: 16px;
  padding: 10px;
  border-radius: 25px;
}

/* Button styling */
.send-button {
  background-color: #0c2640; /* ChatGPT's dark theme */
  color: white;
  border: none;
  padding: 10px 15px;
  border-radius: 50%;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: background-color 0.3s ease;
}

/* Hover effect on the button */
.send-button:hover {
  background-color: #0056b3;
}

/* Input box focus styling */
.search-box:focus {
  outline: none;
  box-shadow: 0px 0px 5px rgba(0, 123, 255, 0.5);
}

.voice-button {
  border: none;
  border-radius: 50%;
  padding: 10px;
  margin-left: 5px;
  cursor: pointer;
  font-size: 16px;
  transition: background-color 0.5s ease; /* Smooth animation for background color */
}

.voice-button:hover {
  background-color: #0056b3; /* Change background color on hover */
}



.assistant-reply-container {
  width: 100%;
  padding: 10px;
  margin-bottom: 20px;
  color: #333;
  background-color: #e9e9e9;
  border-radius: 8px;
  text-align: left; /* Justify text to the left */
  box-shadow: 0px 2px 4px rgba(0, 0, 0, 0.1);
}


.chat-entry {
  margin-bottom: 2em; /* Adds a 2-line gap between each chat entry */

}



/* Gradient progress bar styling */
/* Custom gradient progress bar */
.gradient-progress {
  background: linear-gradient(90deg, #ff7e5f, #feb47b, #86a8e7, #91eae4);
  background-size: 300% 100%; /* Ensures smooth gradient movement */
  animation: gradient-move 3s linear infinite; /* Moving gradient effect */
  height: 5px; /* Adjust thickness */
  border-radius: 4px; /* Rounded edges for better appearance */
}

/* Animation for gradient movement */
@keyframes gradient-move {
  0% {
    background-position: 0% 50%;
  }
  100% {
    background-position: 100% 50%;
  }


}


.input-field {
  background-color: #f7fafc; /* Light gray background */
  width: 100%;
  padding: 1rem 1.25rem; /* Add padding for space inside */
  font-size: 1rem; /* Base font size */
  border: 2px solid #cbd5e0; /* Gray border */
  border-radius: 0.375rem; /* Rounded corners */
  outline: none;
  color: #333;
  transition: border-color 0.3s ease, padding 0.3s ease;
}

.input-field:focus {
  border-color: #3182ce; /* Blue border on focus */
  padding-top: 1.5rem; /* Space for the label to move up */
}

.input-label {
  position: absolute;
  top: 50%; /* Center vertically inside the input */
  left: 1.25rem; /* Align with input padding */
  transform: translateY(-50%); /* Adjust for vertical centering */
  font-size: 1rem; /* Base font size */
  color: #a0aec0; /* Gray text */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
  pointer-events: none; /* Prevent interaction with the label */
}

.input-field:focus + .input-label,
.input-field:not(:placeholder-shown) + .input-label {
  top: -0.1rem; /* Move label above input field */
  font-size: 0.9rem; /* Smaller font size */
  color: #3182ce; /* Blue color when focused */
  background-color: #ffffff; /* Optional: Match background color */
  padding: 0 0.25rem; /* Optional: Padding to match label spacing */
}



/* .chat-history{
  align-items: center;
  justify-content: center;
} */


/* General Style for the Page */
body {
  font-family: Arial, sans-serif;
  color: #fdf8f5;
  text-align: center;
  /* padding: 50px; */
}

/* Style for the Aurora Text Effect with Mixed Gradient */
.aurora-text {
  font-weight: bold;
  background: linear-gradient(90deg, 
    rgba(255, 0, 150, 0.8), /* Pink */
    rgba(0, 204, 255, 0.8), /* Blue */
    rgba(75, 0, 130, 0.8),  /* Indigo */
    rgba(34, 193, 195, 0.8), /* Aqua */
    rgba(253, 29, 29, 0.8),  /* Red */
    rgba(0, 255, 255, 0.8)  /* Cyan */
  );
  background-size: 400% 100%;
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  animation: auroraGlow 6s linear infinite;
}

/* Keyframe for the Aurora Animation (Left to Right, Continuous) */
@keyframes auroraGlow {
  0% {
    background-position: 100% 0%; /* Start from the far left */
  }
  100% {
    background-position: -100% 0%; /* End at the far right */
  }
}

/* Style for the Aurora Items (Optional Styling for the Divs) */
.aurora_item {
  width: 100%;
  height: 20px;
  background: linear-gradient(90deg, 
  rgba(255, 0, 150, 0.8), /* Pink */
  rgba(0, 204, 255, 0.8), /* Blue */
  rgba(75, 0, 130, 0.8),  /* Indigo */
  rgba(34, 193, 195, 0.8), /* Aqua */
  rgba(253, 29, 29, 0.8),  /* Red */
  rgba(0, 255, 255, 0.8)  /* Cyan */
);  margin: 5px 0;
  animation: shimmer 3s ease-in-out infinite;
}

/* Shimmer effect for items */
@keyframes shimmer {
  0% {
    transform: scaleX(0);
    opacity: 0.5;
  }
  50% {
    transform: scaleX(1);
    opacity: 1;
  }
  100% {
    transform: scaleX(0);
    opacity: 0.5;
  }
}


.chat-main-container{
  position: relative;

  display: flex;
  flex-direction: column;  /* Ensures the children are stacked vertically */
  justify-content: flex-start;  /* Aligns items to the top */
  height: 100%;  /* Ensures the container takes full height */
  overflow-y: auto; 
}
.search-container {
  padding: 20px;
  margin-top: auto; /* Pushes the button to the bottom */
  background-color: #918888;  /* Optional background for the button section */
}